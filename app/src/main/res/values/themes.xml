<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MangaApp" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/background_dark</item>
        <item name="colorPrimaryDark">@color/background_dark</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorAccent">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <!-- Apply Tajawal Arabic font globally to all text in the app -->
        <item name="android:fontFamily">@font/tajawal</item>
        <item name="fontFamily">@font/tajawal</item>

        <!-- Smooth fade-in/slide-up transition when opening new activities -->
        <item name="android:windowAnimationStyle">@style/Animation.MangaApp.Activity</item>
    </style>

    <style name="Theme.MangaApp.Transparent" parent="Theme.MangaApp">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Swipe-back theme: translucent window + transparent background so the
         previous activity is revealed as the current one slides away during an
         edge swipe. The content view still fills the window with a solid
         background, so the activity looks normal when NOT being swiped.
         (windowIsOpaque is implied false by windowIsTranslucent=true.) -->
    <style name="Theme.MangaApp.SwipeBack" parent="Theme.MangaApp">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:statusBarColor">@color/background_dark</item>
    </style>

    <!-- RTL activity transitions (Arabic reading direction: forward = leftward).
         OPEN (entering a new screen):
           - new screen slides IN from the LEFT  (left-to-right movement)
           - old screen slides OUT to the RIGHT
         CLOSE (going back):
           - current screen slides OUT to the LEFT  (right-to-left movement)
           - previous screen slides IN from the RIGHT
         This matches the swipe-back gesture (right edge → drag left) so the back
         button and the swipe gesture feel like the same motion. -->
    <style name="Animation.MangaApp.Activity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_right</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_left</item>
    </style>

</resources>
